{
  "name": "mjmlstartertemplate",
  "version": "1.0.0",
  "description": "Starter development environment for the MJML email framework.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": " concurrently \"mjml -w index.mjml -o index-compiled.html --config.minify\" \"browser-sync start --config 'bs-config.js'\"   ",
    "sendTest": "node sendTest"
  },
  "keywords": [
    "mjml"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@sendgrid/mail": "^6.4.0",
    "mjml": "^4.5.1"
  },
  "devDependencies": {
    "browser-sync": "^2.27.10",
    "concurrently": "^5.0.0",
    "dotenv": "^8.2.0"
  }
}
